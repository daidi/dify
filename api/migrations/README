Single-database configuration for Flask.

```cloud
docker compose exec api /bin/bash
flask db revision --autogenerate -m "add data_api"
```

```local
app.py 增加model
```

merge confilct
```local
flask db heads
flask db merge xxx xxx
```

